const computer = {
    brand: 'asus',
    price: 90000,
    processor: 'ryzen',
    ssd: '513gb'
}

// access the keys, keys are known as property
const keys = Object.keys(computer);
console.log(keys);